/*
 * Set a form with the fields name, username, password and gender
 * it will persist on localstorage on every change, when you submit
 * it clears the localstorage, if you didn't hit submit, the data
 * saved on localstorage will load when the component is mounted.
 */
const PersistentForm = () => {};
