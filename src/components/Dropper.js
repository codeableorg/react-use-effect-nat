/*
 * Create a component that adds an event listener to the root document on each render
 * that event listener expects a drop and if the drop happens, then you show the drop
 * data in the console
 */
const Dropper = () => {};
